/**
 * throttle - 일정 시간 간격으로만 실행
 *
 * 🎯 구현할 것:
 * - throttle(fn, limit) 함수 구현
 * - limit 시간 동안은 추가 호출 무시
 *
 * 💭 고민해볼 문제:
 * 1. debounce와 throttle의 차이는?
 *    - debounce: 연속 호출 멈추면 실행
 *    - throttle: 일정 간격으로 실행 보장
 *
 * 2. 스크롤 이벤트에서 debounce를 쓰면 왜 문제일까?
 *
 * 3. requestAnimationFrame과 throttle의 관계는?
 *
 * 4. leading + trailing 옵션을 모두 지원하려면?
 *
 * 📍 사용처:
 * - 스크롤 이벤트 (무한 스크롤)
 * - 마우스 이동 추적
 * - 게임 입력 처리
 */

// 여기에 구현
